# Paste into web/.env when running locally (not Docker) with the model API on port 8001.
# Same settings can go in repo-root .env instead — see .env.example.
#
# USE_MOCK=false
# RAG_SERVICE_URL=http://localhost:8001/query
# REQUEST_TIMEOUT=90