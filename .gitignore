# TerraMind — local/generated paths (see data/README.md for corpus layout)

# === Vector indexes (rebuild with terramind.rag.product.cli / terramind.rag.general.cli) ===
vectorstore/

# === Generated outputs ===
data/eval/runs/
data/processed/

# === Large / non-RAG datasets (not used by web MVP indexes) ===
data/raw/PlantVillage/
data/raw/Fine tuning data/

# === Eval fixtures & sample text (tracked in git) ===
# Negations apply only if a parent rule ignores `data/`; corpus PDFs live under
# `data/raw/documents/` and are tracked normally.
!data/eval/
!data/eval/**
!data/sample/
!data/sample/**

# === Office / editor temp files ===
~$*
.DS_Store

# === Secrets & local IDE ===
.env
.env.local
.env.*.local
.cursorignore

# === Docker ===
# .env is local only (use docker/env.example as template).
# Chroma indexes: vectorstore/ (above) + Compose volume terramind-vectorstore — not committed.

# === Python ===
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# === VS Code ===
.vscode/

# === Node (FrontPage + frontend-react) ===
node_modules/
dist/
.vite/

# === Local scratch (not part of the app) ===
check_openai_key.py
Test.py
catgrouse

# === Dev-only notes (superseded; see docs/PROJECT_STATUS.md) ===
**/claude prompt.md
Project_plan.md
